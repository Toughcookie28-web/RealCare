# Legacy Test Archive

This folder stores the legacy evaluation/test suite that was retired during the evaluation reset.

Archived items were moved from `tests/` to preserve history while rebuilding a new eval stack from scratch.

Do not wire CI to these files.
